import React from 'react';

function Spinner() {
  return (
    <div style={{ textAlign: 'center', padding: '20px' }}>
      <div className="spinner">
        {/* Replace this with any spinner markup or a CSS animation */}
        Loading...
      </div>
    </div>
  );
}

export default Spinner;
